<%= form_for @ingredient do |f| %>
  <table>
  <h1>New Ingredient</h1>
    <tr>
      <td>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.label :description %>
        <%= f.text_area :description %>
      </td>
    </tr>
    <tr>
      <td><%= f.submit 'Create Ingredient' %></td>
    </tr>
  </table>
<% end %>