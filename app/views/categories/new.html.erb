<%= form_for @category do |f| %>
  <table>
  <h1>New Category</h1>
    <tr>
      <td>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.label :description %>
        <%= f.text_area :description %>
      </td>
    </tr>
    <tr>
      <td><%= f.submit 'Create Category' %></td>
    </tr>
  </table>
<% end %>